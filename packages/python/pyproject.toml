[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-workflows"
version = "0.1.0"
description = "Workflow definitions, step sequences, transitions, and run history"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["workflows","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-workflows"
Repository = "https://github.com/mxn2020/minions-workflows"

[tool.hatch.build.targets.wheel]
packages = ["minions_workflows"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
